import { View } from 'react-native';
import { Typograph } from '../../../ui';

export function ListProductsMenu() {
  return (
    <View className="mt-6 flex-1 px-6">
      <Typograph>Menu</Typograph>
    </View>
  );
}
